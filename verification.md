# 验证报告

- 日期：2026-07-22
- 执行者：Codex

## 已验证

- .NET Framework 4.8 WPF 编译成功。
- 本机桥接只接受带 `chrome-extension://` Origin 的 WebSocket 请求。
- 搜索地址只允许绝对 HTTP/HTTPS URL。
- 扩展文件完整，Manifest V3 配置有效。
- 后台加载与点击跳转协议自检通过。
- 已安装的 Edge 扩展与本机程序建立真实连接。
- 使用 Google 的真实后台搜索返回 `PageComplete=Yes`，浏览器加载完成事件验证通过。
- 常驻程序已重启，端口 `127.0.0.1:17891` 状态为 `Established`。
- 快捷键界面已缩减为一个 `760×80` 的 Spotlight 风格玻璃搜索框。
- 搜索引擎、设置、关闭以及加载/完成/错误状态均在搜索框内部显示。
- Windows 明暗主题监听与 WPF 半透明渐变均已实现。
- 玻璃染色已降低到能透出桌面内容的范围，外边框已完全移除。
- 胶囊圆角由 WPF 逐像素透明边缘绘制，未使用会产生硬边锯齿的原生 HWND 区域裁剪。
- 点击加载完成提示后，Edge 已在真实测试中成为前台进程。
- 2026-07-22（Codex）：移除外层黑色投影并在布局完成后重新裁切；原生区域探针确认两个圆角测试点均在窗口区域外。
- 2026-07-22（Codex）：进一步移除会覆盖整个 HWND 的原生 Acrylic；实时窗口捕获确认底层应用可穿过玻璃层和圆角区域显示。
- 2026-07-22（Codex）：玻璃层已加深，同时保持圆角外逐像素透明；`SelfTestExit=0`，扩展连接正常。
- 2026-07-22（Codex）：加入外观设置持久化、移除 X、失焦隐藏和引擎滚轮切换；真实设置保存、编译、自检及扩展连接均通过。
- 2026-07-22（Codex）：结合两个指定技能完成四层 Liquid Glass 重设计、主题化圆角菜单和 Fluent 图标；不透明度支持 0%–100%，真实窗口与边界自检通过。
- 2026-07-22（Codex）：移除镜面高光和原生圆角硬裁剪；保留标准胶囊圆角，并改用 WPF 逐像素透明抗锯齿边缘。
- 2026-07-22（Codex）：重新编译及 `--self-test` 退出码均为 `0`；常驻进程已恢复，Edge 扩展连接数为 `1`。
- 2026-07-23（Codex）：第二版将当前搜索引擎名称内嵌到输入框左侧，保留点击菜单与鼠标滚轮切换；真实窗口完成 `Google → 百度 → Google` 滚轮复测，编译、自检和扩展连接均通过。
- 2026-07-23（Codex）：按反馈取消引擎按钮与下拉菜单；当前引擎名称改为输入框空白时的占位提示，仅允许在输入区使用鼠标滚轮切换；真实窗口验证提示切换、输入隐藏及清空恢复均正常。
- 2026-07-23（Codex）：移除“页面已加载，点击打开”可见文案；加载完成后保留原搜索内容，将整个搜索框切换为手形光标和点击打开行为。
- 2026-07-23（Codex）：使用真实 Google 查询完成端到端复测；完成态仅保留搜索内容，无“页面已加载/点击打开”文案，点击输入区后 `BrowserActivated=Yes`、`ForegroundProcess=msedge`，常驻程序与扩展连接已恢复。
- 2026-07-23（Codex）：后台加载改为搜索框整圈橙色呼吸灯，完成后切换为绿色常亮；真实窗口确认动画亮度变化、无加载文案且圆角边缘连续，点击绿色状态后 `BrowserActivated=Yes`、`ForegroundProcess=msedge`。
- 2026-07-23（Codex）：增强呼吸反馈至边缘和完整玻璃内部同步闪烁，橙色强度在 `0.02–0.85` 间变化且至少持续一个 `0.9s` 周期；真实窗口已捕获暗态、橙色峰值和绿色完成态。
- 2026-07-23（Codex）：按反馈移除内部区域灯效，仅保留边框以 `0.05–1` 强度呼吸并在完成后变绿；真实窗口确认玻璃内部从加载到完成均保持原有材质。
- 2026-07-23（Codex）：移除设置按钮悬停、按下和焦点状态的椭圆背景，仅保留齿轮图标透明度反馈；真实点击展开菜单后未见椭圆残留。
- 2026-07-23（Codex）：设置图标向左移动 `6px`，真实窗口确认右侧留白由约 `11px` 增至约 `17px`。
- 2026-07-23（Codex）：设置图标再左移 `10px`，右侧留白约为 `27px`；背景改为多节点 ScRGB 渐变，真实窗口复查过渡连续。
- 2026-07-23（Codex）：主背景进一步改为无中间节点的单段 ScRGB 色场，真实窗口未见渐变切线；设置子菜单已统一为同色 Liquid Glass、18px 圆角、细描边和蓝色低透明选中态。
- 2026-07-23（Codex）：搜索框增加圆角裁切的真实桌面取样与可调 Gaussian Blur（0–40），外观设置页统一为无系统边框的 Liquid Glass 面板；编译、`--self-test`、`--live-test` 均为退出码 `0`，滑块保存后读取 `BlurRadius=28`，常驻扩展连接数为 `1`。
- 2026-07-23（Codex）：动态捕获试验因可见亮度异常已完整撤回，恢复上一版静态背景取样；最终编译和自检退出码为 `0`，常驻扩展连接数为 `1`。
- 2026-07-23（Codex）：设置菜单删除“外观设置”下方分隔线；外观页移除副标题及两条说明提示，改为标签和值同排、滑块在下的单层玻璃布局；实机截图、自检和扩展重连均通过。
- 2026-07-23（Codex）：设置菜单已更名为“通用设置 / 搜索引擎 / 加载设置”；通用页缩为 `430×374` 并独立居中、自动避让搜索框。
- 2026-07-23（Codex）：默认唤醒键改为 `Ctrl+Space`；系统级实测窗口状态 `True → False → True`，设置页也可录制新组合键并在冲突时恢复旧键。
- 2026-07-23（Codex）：搜索引擎管理页支持新增、删除、上下排序、名称和地址编辑、默认项选择；自动化交互验证编辑框 `6 → 8 → 6` 且取消不会写入配置。
- 2026-07-23（Codex）：最终 `--self-test` 为 `SelfTest=OK`、`SettingsPersistence=OK`、`BridgeProtocol=OK`；`--live-test` 为 `LiveTest=OK`、`PageComplete=Yes`。
- 2026-07-23（Codex）：新版常驻程序已恢复，进程 ID `56064`，Edge 扩展连接为 `Established`。
- 2026-07-23（Codex）：通用设置的唤醒快捷键、深色背景和浅色背景值框已统一为 `12px` 圆角矩形；编译、自检和实机渲染检查通过。
- 2026-07-23（Codex）：三个值框统一为 `150×34`，内容水平和垂直居中；自动化测量及实机截图验证通过。
- 2026-07-23（Codex）：全部保存、取消、添加按钮统一为 `80×34` 和 `12px` 中圆角；保存/取消位于右下角，添加位于搜索引擎页标题右侧，编译、自检及两页实机预览通过。
- 2026-07-23（Codex）：通用设置新增开机自启动和动态高斯模糊复选框；动态模糊联动 10/30/60 FPS，默认关闭并保留默认 10 FPS。
- 2026-07-23（Codex）：动态刷新仅在搜索框可见时运行；显示亲和性实测为可见 `0x11`、隐藏 `0x0`、再次唤出 `0x11`，避免捕获自身造成递归叠亮。
- 2026-07-23（Codex）：开机启动真实写入当前用户 `Run` 项成功，失败回滚路径已接通；验证结束后启动项已恢复关闭。
- 2026-07-23（Codex）：生成透明 `QuickSearchFloat-icon.png` 和包含 16–256px 七种尺寸的 `QuickSearchFloat.ico`；最终 EXE 已嵌入图标，主窗口和托盘同步使用。
- 2026-07-23（Codex）：最终编译与 `--self-test` 退出码均为 `0`，输出含 `DynamicBlur=False`、`DynamicBlurFps=10`、`SettingsPersistence=OK`；`--live-test` 为 `LiveTest=OK`、`PageComplete=Yes`。
- 2026-07-23（Codex）：新版常驻程序已恢复，进程 ID `17596`、响应正常，Edge 扩展连接为 `Established`。
- 2026-07-23（Codex）：设置菜单已删除“加载设置”，实际 UI Automation 菜单仅含“通用设置”和“搜索引擎”；编译、自检和静态残留检查通过，新版常驻进程 ID `41392`，Edge 扩展连接正常。
- 2026-07-23（Codex）：v2.1.0 发布前最终编译、自检和真实后台加载通过；默认配置为 `DynamicBlur=False`、`DynamicBlurFps=10`，`LiveTest=OK`、`PageComplete=Yes`。
- 2026-07-23（Codex）：内嵌结果面板已完成最终验证。AI 结果在存在时全宽优先，普通结果双列瀑布流排列，明确广告被过滤；Google 新语义链接、必应和百度下一页均有自动化回归。
- 2026-07-23（Codex）：最终 EXE SHA-256 为 `AD45140E2FA73CFB13B6B160F667063081BEABBE4454ABA3B5031474C2545B81`；`--self-test` 退出码 `0`，`SettingsPersistence=OK`、`BridgeProtocol=OK`。
- 2026-07-23（Codex）：真实搜索结果为 Google `5` 条、必应 `20` 条、百度 `19` 条；必应和百度均完成第二批加载，Google 当前测试页无下一页时正确停止。
- 2026-07-23（Codex）：AI 结果已改为纯文字且不可点击；合成测试确认重复“AI 概览”、引用链接文字和 URL 均已移除。
- 2026-07-23（Codex）：普通结果 favicon 已通过 Apple/Microsoft 真实 HTTPS 图标验证，缺失 `/favicon.ico` 的站点显示域名首字母；最终自检与 Google 真实搜索均通过。
- 2026-07-23（Codex）：Google 的“无法针对此搜索生成 / 目前无法生成 AI 概要 / 请稍后重试”不再作为 AI 答案显示；纯提示被丢弃，提示后有有效正文时仅保留正文，合成回归与真实搜索均通过。
- 2026-07-23（Codex）：结果瀑布流材质层已增加 28px 圆角裁切；预览溢出数据从顶部滚到 100% 后，卡片仍被限制在玻璃外框内。
- 2026-07-23（Codex）：结果描边已置于滚动内容上层，滚动视口内缩 2px；卡片滚动到边缘时不再覆盖玻璃框线。
- 2026-07-23（Codex）：网页结果现默认折叠在搜索框下方的独立玻璃下拉条中；有 AI 时先显示 AI，展开后才显示网页瀑布流并继续加载。
- 2026-07-23（Codex）：结果就绪后搜索框为点击目标，点击或按 Enter 打开完整搜索页；每次重新唤醒会清空查询、结果、滚动和展开状态。
- 2026-07-23（Codex）：折叠→展开→收起 UI Automation、`WM_HOTKEY` 唤醒清空、最终编译和 `--self-test` 均通过；常驻进程 ID `34632` 响应正常，Edge 扩展连接为 `Established`。
- 2026-07-23（Codex）：下拉展开/收起已改为同帧完成背景替换与布局，不再显示旧背景位图拉伸的中间帧；隐藏前会先提交空白会话布局，再次唤醒 120ms 探针未发现旧结果。
- 2026-07-23（Codex）：网页结果条与首行卡片之间增加 12px 逻辑间距，125% 缩放下实测为 15px；最终自检通过，常驻进程 ID `28272` 与 Edge 扩展连接正常。
- 2026-07-23（Codex）：下拉展开/收起前会按目标高度预先生成模糊背景，不再先清空当前高斯模糊层；捕获异常时保留上一帧。展开事件返回后的第一帧截图确认内容框模糊连续，正式自检通过，常驻进程 ID `41052` 与 Edge 扩展连接正常。
- 2026-07-23（Codex）：v3.0.0 发布构建通过 C# 编译、扩展 `node --check`、Manifest 3.0.0 校验、内置 `--self-test` 与 `git diff --check`；正式 EXE SHA-256 为 `CEDFA08E9E7819C764D4DD2E648CDCBAE5C9E6F9345A5AC17480753D1256F9BB`。

## 结论

本地扩展方案已完成安装、三引擎真实结果提取、瀑布流展示、分页加载及常驻连接验证。
